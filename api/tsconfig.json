{
  "comment": "tsconfig usato da @vercel/node per compilare la Function /api. Allinea la module resolution dell'api-server (bundler, import senza estensione) ed espone i tipi Node, evitando i default nodenext di @vercel/node.",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "target": "es2022",
    "lib": ["es2022"],
    "types": ["node"],
    "skipLibCheck": true,
    "allowJs": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "strict": false,
    "noImplicitAny": false,
    "customConditions": ["workspace"],
    "noEmit": true
  },
  "include": [
    "**/*.ts",
    "../artifacts/api-server/src/**/*.ts",
    "../lib/**/*.ts"
  ]
}
